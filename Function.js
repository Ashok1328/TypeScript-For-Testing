"use strict";
// Named Function
function display() {
    console.log("Welcome To TypeScript");
}
display();
// -------- Named Function include parameter types and return type --------------
function Sum(x, y) {
    return x + y;
}
Sum(50, 200);
